#include <stdio.h>
#include<stdlib.h>
 int trouveval(int  t[], int val){
    int p, i;
    for(i=0,i<lenght.t,i++){
        if(t[i]==val){
            p==i;
        }
        if(p==0) return p;
        else return (-1);
    }
 }
 void main({
    int tab [];
    int i, n,x,rs;
     printf("donner la taille de votre tableau :");
     for(i=0,i<n,i++){
        printf("donner la valeur :\n");
        scanf("%d",&x);
        tab[i]=x;
    }
    printf("donner la valeur :\n");
    scanf("%d",&x);
    rs=trouveval(t[n],x)
    print("la position est : " rs)
 })